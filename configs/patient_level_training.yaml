patient_level_training:
  patient_count: 10
  validation_fraction: 0.2
  epoch_count: 20
  batch_size: 1
  num_workers: 0
  pin_memory: true
  random_seed: 42
  base_channels: 8
  learning_rate: 0.001
  weight_decay: 0.00001
  lr_scheduler_factor: 0.5
  lr_scheduler_patience: 8
  early_stopping_patience: 20
  early_stopping_minimum_improvement: 0.001
  device: auto
  checkpoint_path: artifacts/checkpoints/patient_level_training.pt
